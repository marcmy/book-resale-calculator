name: Build and Release

on:
  workflow_dispatch:
    inputs:
      release:
        description: "Create a GitHub release when the version tag does not already exist"
        type: boolean
        default: true
  push:
    branches:
      - main
    paths:
      - ".github/workflows/build-release.yml"
      - "package.json"
      - "package-lock.json"
      - "index.html"
      - "styles.css"
      - "script.js"
      - "rates.js"
      - "electron/**"
      - "scripts/check-calculator.js"

permissions:
  contents: write

concurrency:
  group: build-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-windows:
    name: Build Windows installer
    runs-on: windows-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
      tag: ${{ steps.meta.outputs.tag }}
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: false
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Determine package version
        id: meta
        shell: pwsh
        run: |
          $version = node -p "require('./package.json').version"
          if ([string]::IsNullOrWhiteSpace($version)) {
            throw "package.json version is empty."
          }

          "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          "tag=v$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8

      - name: Install dependencies
        run: npm ci

      - name: Run calculator checks
        run: npm run check

      - name: Build Windows installer
        run: npm run build:win -- --publish never

      - name: Verify release assets
        shell: pwsh
        run: |
          $assets = Get-ChildItem -Path dist -File -Include *.exe,*.blockmap,latest*.yml -Recurse
          if (-not $assets) {
            throw "No installer release assets were created under dist/."
          }

          $assets | ForEach-Object { $_.FullName }

      - name: Upload Windows installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: Book-Resale-Calculator-win-${{ steps.meta.outputs.version }}
          if-no-files-found: error
          path: |
            dist/*.exe
            dist/*.blockmap
            dist/latest*.yml

  release:
    name: Publish GitHub release
    runs-on: ubuntu-latest
    needs: build-windows
    if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.release) }}
    permissions:
      contents: write
    steps:
      - name: Download Windows installer artifact
        uses: actions/download-artifact@v4
        with:
          path: release-assets

      - name: Create release if needed
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.build-windows.outputs.tag }}
          VERSION: ${{ needs.build-windows.outputs.version }}
        shell: bash
        run: |
          set -euo pipefail

          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release $TAG already exists; skipping release creation."
            exit 0
          fi

          mapfile -d '' assets < <(find release-assets -type f -print0 | sort -z)
          if [ "${#assets[@]}" -eq 0 ]; then
            echo "No release assets found." >&2
            exit 1
          fi

          printf 'Release assets:\n'
          printf '  %s\n' "${assets[@]}"

          gh release create "$TAG" "${assets[@]}" \
            --target "$GITHUB_SHA" \
            --title "Book Resale Calculator $TAG" \
            --notes "Automated Windows installer release for Book Resale Calculator $VERSION."
